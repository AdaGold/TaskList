<h1>Tasks List</h1>
<ul>
  <%@tasks.each do |task|%>
    <li>
      <%= link_to task.name, task_path(task) %>
      <%= button_to "Mark complete", task_complete_path(task), method: :patch%>

    </li>
  <%end%>
</ul>