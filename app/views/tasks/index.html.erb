<h1>Here is a list of your tasks today!</h1>
  
<% @tasks.each do |task| %>
  <h3><%= link_to task.title, show_path(task) %>: <%= task.description %></h3> 
  <%= button_to  "Delete Task", destroy_path(task), method: :delete %>
<% end %>
