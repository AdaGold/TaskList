<h1>Edit your tasks!</h1>

<%= form_for @task, url: update_path, method: :put do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.submit %>

<% end %>