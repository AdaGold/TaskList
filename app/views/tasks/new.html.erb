<%= form_with model: @task do |f| %>
  <p>Please provide the following info to add a new task:</p>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div>
  <%= f.label :description %>
  <%= f.text_field :description %>
  </div>


  <%= f.submit "Save Task" %>

<% end %>